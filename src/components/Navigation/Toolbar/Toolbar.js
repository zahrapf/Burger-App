import React from 'react';
import Logo from '../../Logo/Logo';
import './Toolbar.css';


const toolbar = (props) => (
    <header className='Toolbar'>
        <Logo />
        <div>MENU</div>
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;